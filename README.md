# EdgeCrdt (WIP)

Edge-CRDT provides a bounded, topology-aware CRDT runtime for edge and
IoT gateways, with delta-based replication tuned for unreliable,
asymmetric links. It is not a generic "CRDT for everything" library and
not intended for tiny bare-metal MCUs.
